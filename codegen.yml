overwrite: true
schema: "./src/schema.graphql"
generates:
  "src/graphql.ts":
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      useIndexSignature: true,
      contextType: "../types#MyContext"
